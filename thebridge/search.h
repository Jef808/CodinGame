#ifndef SEARCH_H_
#define SEARCH_H_



/**
 * struct representing the moves at the root. They need to be kept in a priority list
 * in case we need to output a 'best-guess move' early, and can be used to index
 * information retrieved from earlier searches in their respective subtrees.
 *
 * One of the nodes following their corresponding edges will become the next global root
 * at the next move.
 */
struct RootMove {

};

void search()




#endif // SEARCH_H_
